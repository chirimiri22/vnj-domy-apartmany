import { Box, Container, Typography } from "@mui/material";

export const Standards = () => {
  return (
    <Container>
      <Typography variant={"h2"}>Standardy bytů</Typography>
      cisla
      <Box sx={{ height: 500, textAlign: "center" }}>todo: galerie</Box>
    </Container>
  );
};
